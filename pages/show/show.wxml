<!--pages/show/show.wxml-->
<view class="food-photo-container">
  <image class="food" src="{{ post.image }}"></image>
</view>


<view class="product-container">
  <view class="product-name">
    {{post.name}}
  </view>
    <view class="product-price">
      <text class="new-price">¥{{ post.new_price }}</text> 
      <text class="old-price">¥{{ post.original_price }}</text>
    </view>
    <view class="product-description">
      {{post.description}}
    </view>
  <view class="flexbox">
    <image class="clock" src="https://image.flaticon.com/icons/svg/59/59252.svg">
    </image>
      <view class="time">
        <view class="time-table">Pickup between {{post.start_time}} - {{post.end_time}}</view>
      </view>
      
   
    <!-- <view class="flexbox">
      <view class="right-content">
        <map id="myMap"
          latitude="{{latitude}}"
          longitude="{{longitude}}"
          markers="{{markers}}"
          covers="{{covers}}"
          show-location>
        </map>
      </view>  
    </view>  -->
  </view>   
</view>

<view class="restaurant-card" bindtap='jumpToRestaurant'>
 <view class="restaurant-avatars">
  <image class="restaurant-avatar" src="{{restaurant.image}}"></image>
  </view>
  <view class="rpostpostposddfdfdfestaurant-info">
    <view class="restaurant-name">{{restaurant.name}}</view>
    <view class="restaurant-cuisine">{{restaurant.cuisine}}</view>
    <view class="restaurant-address">{{restaurant.address}}</view>

  </view>
</view>
<button class="weui-btn" type="primary" bindtap='jumpToClaim'>Claim it!</button> 